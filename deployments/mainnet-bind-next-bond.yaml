---
id: 0
name: Bind the next bond
network: mainnet
stacks-node: "https://api.hiro.so"
bitcoin-node: "http://blockstack:blockstacksystem@bitcoin.blockstack.com:8332"
plan:
  batches:
  - id: 0
    transactions:
    # bind-next-bond() -- permissionless, no arguments. The pool takes the
    # earliest bond pox-5 has set up with `esbee-dao-bond-staker-1` allowlisted
    # whose notice can still run out before its stake window opens;
    # `find-next-bond` says which one that is, and `none` means the call would
    # fail. Run `pnpm run probe:mainnet` first: it prints the bind deadline.
    # For the genesis bond (index 1, starts 966350) that is burn 965386.
    - transaction-type: contract-call
      contract-id: SPFCGF789WX1B737VQYAQ6BG3QYVMJGPDKRKYK00.esbee-dao-bond-staker-1
      expected-sender: SPFCGF789WX1B737VQYAQ6BG3QYVMJGPDKRKYK00
      method: bind-next-bond
      parameters: []
      cost: 50000
    epoch: '4.0'
