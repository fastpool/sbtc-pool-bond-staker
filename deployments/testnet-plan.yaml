---
id: 0
name: Testnet deployment
network: testnet
stacks-node: "https://api.testnet.hiro.so"
bitcoin-node: "http://blockstack:blockstacksystem@bitcoind.testnet.stacks.co:18332"
plan:
  batches:
  - id: 0
    transactions:
    - transaction-type: contract-publish
      contract-name: bond-treasury-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 50000
      path: build/testnet/bond-treasury-3.clar
      clarity-version: 6
    - transaction-type: contract-publish
      contract-name: iou-bond-btc-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 50000
      path: build/testnet/iou-bond-btc-3.clar
      clarity-version: 6
    - transaction-type: contract-publish
      contract-name: iou-bond-stx-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 50000
      path: build/testnet/iou-bond-stx-3.clar
      clarity-version: 6
    - transaction-type: contract-publish
      contract-name: vault-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 659610
      path: build/testnet/vault-3.clar
      clarity-version: 6
    - transaction-type: contract-publish
      contract-name: bond-bridge-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 276570
      path: build/testnet/bond-bridge-3.clar
      clarity-version: 6
    - transaction-type: contract-publish
      contract-name: esbee-dao-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      cost: 116980
      path: build/testnet/esbee-dao-3.clar
      clarity-version: 6
    epoch: '4.0'
  - id: 1
    transactions:
    # Binds the pool to its signer manager and names its operator. Only the
    # contract's own deployer may call it, and only once. A separate batch so
    # the publishes above are confirmed first.
    #
    # The deployer takes the seat rather than the DAO because the DAO cannot
    # vote until the pool has staked: voting weight is committed shares, and
    # there are none before then. The DAO joins as a second operator in the
    # next batch. Binding itself needs no seat -- `bind-next-bond` is
    # permissionless -- so what the seat holds is the signer manager, the
    # skip, and the sweep.
    - transaction-type: contract-call
      contract-id: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM.vault-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      method: initialize
      parameters:
        - "'ST1B38CGQRPXEMRH7B66VXTS22DQTNMSW4YJJ7QK1.signer-manager"
        - "'STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM"
      cost: 50000
    epoch: '4.0'
  - id: 2
    transactions:
    # Seats the DAO alongside the deployer, which is what puts the voted
    # powers -- signer moves, the trusted list, who the operators are, sweeps
    # and skipping a bond -- in the members' hands.
    #
    # `update-operator` refuses to change the caller's own entry, so the
    # deployer cannot retire itself here. Handing over completely is a later
    # call *from the DAO*, by vote, once the pool has staked and the members
    # have the shares to vote with.
    - transaction-type: contract-call
      contract-id: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM.vault-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      method: update-operator
      parameters:
        - "'STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM.esbee-dao-3"
        - "true"
      cost: 50000
    epoch: '4.0'
