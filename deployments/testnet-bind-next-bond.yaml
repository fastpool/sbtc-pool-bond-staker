---
id: 0
name: Bind the next bond
network: testnet
stacks-node: "https://api.testnet.hiro.so"
bitcoin-node: "http://blockstack:blockstacksystem@bitcoind.testnet.stacks.co:18332"
plan:
  batches:
  - id: 0
    transactions:
    # bind-next-bond() -- permissionless, no arguments. The pool takes the
    # earliest bond pox-5 has set up with `vault-3` allowlisted whose notice
    # can still run out before its stake window opens; `find-next-bond` says
    # which one that is, and `none` means the call would fail. Run
    # `node scripts/probe-testnet.mjs` first: it prints the bind deadline.
    - transaction-type: contract-call
      contract-id: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM.vault-3
      expected-sender: STFCGF789WX1B737VQYAQ6BG3QYVMJGPDJN4TJFM
      method: bind-next-bond
      parameters: []
      cost: 50000
    epoch: '4.0'
